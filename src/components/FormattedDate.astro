---
interface Props {
  date: Date;
}
const { date } = Astro.props;
const formatted = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---
<time datetime={date.toISOString()} class="text-[color:var(--color-ink-muted)] text-[14px]">
  {formatted}
</time>
